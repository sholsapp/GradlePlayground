pluginManagement {
  repositories {
    ivy {
      url "file:////tmp/ivy-repo"
      patternLayout {
        artifact "[organization]/[module]/[revision]/[artifact]-[revision].[ext]"
      }
    }
    gradlePluginPortal()
  }
}

rootProject.name = 'untitled'